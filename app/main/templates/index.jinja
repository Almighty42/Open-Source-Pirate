{% extends "./base.jinja" %}
{% block content %}
    <div class="articles">
        {% for article in articles %}
            <div class="article">
                <h2>{{ article.title }}</h2>
                <p>{{ article.created_at }}</p>
                <a href="/article/{{ article.id }}">View more</a>
            </div>
        {% endfor %}
        <div>
        {% endblock %}
